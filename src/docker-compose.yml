services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: energycomm
      POSTGRES_PASSWORD: energycomm
      POSTGRES_DB: energycomm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U energycomm"]
      interval: 5s
      timeout: 3s
      retries: 5

  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://energycomm:energycomm@postgres:5432/energycomm
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - .:/app

volumes:
  pgdata:
